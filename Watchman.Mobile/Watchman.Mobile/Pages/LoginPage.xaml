<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Watchman.Mobile.Pages.LoginPage">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="0.1*"/>
                <RowDefinition Height="0.35*"/>
                <RowDefinition Height="0.35*"/>
                <RowDefinition Height="0.2*"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" TextColor="Red" Text="{Binding Error}"/>
            <StackLayout Grid.Row="1" Margin="20, 10, 20,10">
                <Label x:Name="LblEmail" Text="Email"/>
                <Entry x:Name="EntryEmail" Text="{Binding Email}"/>
            </StackLayout>
            <StackLayout Grid.Row="2" Margin="20, 10, 20,10">
                <Label x:Name="LblPassword" Text="Password"/>
                <Entry x:Name="EntryPassword" Text="{Binding Password}"/>
            </StackLayout>
            <StackLayout Grid.Row="3">
                <Button x:Name="BtnSubmit" Text="Log in" Command="{Binding SubmitCommand}"/>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>